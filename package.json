{
  "name": "voice-notifications",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "TEST_PORT=3098 npx playwright test",
    "test:notifications": "TEST_PORT=3098 npx playwright test tests/notifications.spec.mjs"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
